// placeholder_pages.dart
//
// Proje yapısını kurarken geçici olarak kullanılan boş sayfa sınıfları.
// Bu dosya, tüm sayfalar gerçek implementasyonlarla değiştirildiğinde kaldırılacaktır.
//
// Yazan: Deniz Dogan
// Tarih: 2025-07-18

import 'package:flutter/material.dart';

// Auth Pages
/*
class LoginPage extends StatelessWidget {
  const LoginPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Giriş Sayfası')),
    );
  }
}

class ForgotPasswordPage extends StatelessWidget {
  const ForgotPasswordPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Şifremi Unuttum Sayfası')),
    );
  }
}

// Onboarding Pages
class FaceRecognitionPage extends StatelessWidget {
  const FaceRecognitionPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Yüz Tanıma Sayfası')),
    );
  }
}

class AlcoholTestPage extends StatelessWidget {
  const AlcoholTestPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Alkolmetre Sayfası')),
    );
  }
}

class ChecklistPage extends StatelessWidget {
  const ChecklistPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Checklist Sayfası')),
    );
  }
}

class ApprovalWaitingPage extends StatelessWidget {
  const ApprovalWaitingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Onay Bekleme Sayfası')),
    );
  }
}

// Flight Pages
class SensorDashboardPage extends StatelessWidget {
  const SensorDashboardPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Sensör Gösterge Paneli')),
    );
  }
}



class AlertPage extends StatelessWidget {
  const AlertPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Uyarı Sayfası')),
    );
  }
}

class ReportIssuePage extends StatelessWidget {
  const ReportIssuePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Sorun Bildirme Sayfası')),
    );
  }
}

class EndFlightPage extends StatelessWidget {
  const EndFlightPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Uçuş Sonlandırma Sayfası')),
    );
  }


}

 */